<arquillian
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://jboss.org/schema/arquillian"
        xsi:schemaLocation="http://jboss.org/schema/arquillian
    http://jboss.org/schema/arquillian/arquillian_1_0.xsd">
    <!--    <defaultProtocol type="Servlet 5.0"/>-->

    <container qualifier="liberty-remote">
        <configuration>
            <property name="hostName">localhost</property>
            <property name="serverName">defaultServer</property>
            <property name="username">admin</property>
            <property name="password">admin</property>
            <property name="httpPort">9080</property>
            <property name="httpsPort">9443</property>
        </configuration>
    </container>
    <!-- No Docker Container -->
    <!-- server create defaultServer -->
    <!-- server start defaultServer -->
    <!-- server stop defaultServer -->
    <!-- copy server.xml to  usr/servers/defaultServer -->
    <!-- export cert from  usr/servers/defaultServer/resources/security/key.p12  #password is from server.xml-->
    <!-- import cert from  jre/lib/security/cacerts #default password is changeit -->
    <!--\ No Docker Container -->
    <!-- Docker Container -->
    <!-- No Success Yet -->
    <!--\ Docker Container -->

</arquillian>